version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/  # Common directory for web servers like Apache

hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
